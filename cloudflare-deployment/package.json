{
  "name": "creative-thinking-cloudflare",
  "version": "1.0.0",
  "description": "Creative Thinking MCP Server deployed on Cloudflare Workers with Agents SDK",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.6",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "agents": "^0.0.113",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "wrangler": "^4.32.0"
  }
}
