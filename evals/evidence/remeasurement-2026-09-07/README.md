# Re-measurement transcripts, 7 Sep 2026

Inputs and finals for the `neuro_computational` vs `quantum_superposition` run described in
`../technique-contrasts.md` (section "Re-measurement, 7 Sep 2026"). `steps-*.json` hold the step
outputs and the structured fields sent to `socketes execute`; `*-step5.json` / `*-step4.json` are
the server's final responses; `plan-*.json` are the plans the steps ran under.
